# ============================================================================
# 业务知识映射配置
# ============================================================================
# 用途: 将业务流程编号和业务规则编号映射到对应的名称
# 维护: 当添加新的业务流程或规则时，需要在此文件中添加映射关系
# ============================================================================

# 业务流程映射 (Flow ID -> Flow Name)
business_flows:
  TKF_001: "离线剧本批量生成"
  TKF_002: "实时剧本生成"
  TKF_003: "剧本推荐结果查询"

# 业务规则映射 (Rule ID -> Rule Name)
business_rules:
  TKR_001: "DP数据产出状态判断"
  TKR_002: "Job断点续传规则"
  TKR_003: "剧本生成状态判断"
  TKR_004: "AI平台调用重试规则"
  TKR_005: "剧本缓存策略"

